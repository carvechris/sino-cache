logging:
    level:
        com.whb.cache: DEBUG
#        org.springframework.core: DEBUG
    path: /log

#redis-cache 相关
redis:
    pool:
         maxActive: 300
         maxIdle: 100
         maxWait: 1000
    host: 127.0.0.1
    port: 6379
    password:
    timeout: 2000
    sysName: sino
    enable: true
    database: 0
server:
  port: 8081